const config = {
    plugins: ["@tailwindcss/postcss"],
    theme: {
        extend: {
            screen: {
                lg: '1025px',
                md:'770px',
            }
        },
    },
};

export default config;